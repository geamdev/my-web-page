export interface ITheme {
  theme: {
    colors: {
      primary?: string;
      background?: string;
      card?: string;
      text?: string;
      border?: string;
      notification?: string;
    };
  };
}
